page[dom_id(@attachment)].visual_effect :blind_up, :duration => 0.25

if called_from_landing_page?(:accounts)
  page << refresh_sidebar_for(:accounts, :show, :summary)
elsif called_from_landing_page?(:contact_groups)
  page.replace_html :email_group_link, "#{mail_to(@current_user.email, 'Email group', :bcc => entity.email_addresses)} | "
elsif called_from_landing_page?(:campaigns)
  page << refresh_sidebar_for(:campaigns, :show, :summary)
end
